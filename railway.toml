services:
  - type: web
    name: task-management-api
    env: go
    plan: free
    region: singapore
    buildCommand: go mod download && go build -o main
    startCommand: ./main
    envVars:
      - key: PORT
        value: 8080 