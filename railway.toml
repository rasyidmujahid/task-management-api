[service]
type = "web"
name = "task-management-api"
env = "go"
plan = "free"
region = "singapore"
buildCommand = "go mod tidy && go build -o main"
startCommand = "./main"

[service.envVars]
PORT = "8080"

[build]
builder = "nixpacks" 